num_dict = {}

provide_list = [2,3,4,2,4]

for element in provide_list:
    if num_dict.items == element:
        num_dict[element] += 1
    else:
        num_dict[element] = 1

print(num_dict)
